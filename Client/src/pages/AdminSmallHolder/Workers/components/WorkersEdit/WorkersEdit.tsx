import classNames from 'classnames/bind';

import styles from './WorkersEdit.module.scss';

const cx = classNames.bind(styles);
type props = {}
const WorkersEdit = (props: props) => {
    return <div className={cx('wrapper')}> WorkersEdit</div>;
}

export default WorkersEdit;
