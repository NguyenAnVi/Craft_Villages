import classNames from 'classnames/bind';

import styles from './WorkersDetail.module.scss';

const cx = classNames.bind(styles);
type props = {}
const WorkersDetail = (props: props) => {
    return <div className={cx('wrapper')}> WorkersDetail</div>;
}

export default WorkersDetail;
